import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'control-volumetrico',
  webDir: '../../dist/apps/control-volumetrico',
  bundledWebRuntime: false,
};

export default config;
